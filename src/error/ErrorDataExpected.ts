import ErrorData from "./ErrorData"

/**
 * The expected data error.
 *
 * Errors related to the data validation itself, e.g. invalid data type.
 */
export default abstract class ErrorDataExpected extends ErrorData {

}
